#!/usr/bin/env bash

# BUG: THRE IS NO WAY TO SOURCE A FILE.
#      i3blocks does not even respect which environment
# that is running the script, always run with user default
# shell script.
# ANY VARS DECLARED IN ANOTHER SOURCE FILE WON'T WORK.
# . sys.conf

fa_circle_o='\U000f10c'
# fa_clock_o='\U000f017' NOT WORKING IN FONTS-AWESOME 4.7

hour_min=`date '+%T'`
sec=${hour_min##[0-9]*:}
hour_min=${hour_min%:*[0-9]}
color='#fdfefe'

if [ $(($sec % 2)) -eq 0 ]; then
	color='#17202a'
fi

echo -e "<span foreground='#53ff1a' size='x-large' font_desc='EmojiSymbols'>$fa_circle</span>"\
	 "<span foreground='#53ff1a' size='large' font_desc='EmojiSymbols'>$hour_min</span><span foreground='$color' size='small'>:</span><span foreground='#53ff1a' size='x-small'>$sec</span>"

exit 0
